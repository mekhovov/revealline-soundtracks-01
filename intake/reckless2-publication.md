# Reckless 2 publication binding

This guarded publication consumes only the exact hosted artifact prepared by
source PR #22. It never reacquires or re-encodes audio. It preserves all native
sources, source/licence evidence, receipts, pending review flags and exact MP3
derivatives in an immutable intake archive, then creates one four-recording
listening-only batch on the accepted 136-recording archive baseline.

- Source run: `36193952191`
- Artifact: `10889860079`
- Artifact bytes: `20,781,317`
- Artifact SHA-256: `00211751f1701748948ca922c589120ad15db47b512b7bab15ca691a082bbc72`
- Source head: `fec06dd45eacea56d00acbd470a43999b2f22710`
- Hosted PR runner/tree proof: `2d2bbda086833d2b142dddbfa5668639683ddc1f`
- Source tree: `704ac13c13b3c408bf5bc7ecc87df6d9827aa878`
- Source base: `824e34e4957ab29b7ef841115f631a579f741fc4`
- Merged source PR: `4f9ac1c1179d768bb851b63dd8cee6a604897fc5`

The artifact receipt and immutable Actions log bind the runner revision to the
source head, base, tree, workflow and run. The remote publication gate verifies
those objects and the artifact metadata before downloading any hosted bytes.
It also verifies the exact merged source commit, tree and ordered parents; that
merge must be an ancestor of the publication checkout. The accepted 136-row
catalogue is loaded from that exact merged Git object and pinned by exact file
digest before any generation begins. Source tests accept that exact base and,
after hosted assembly, only its unchanged 136-row prefix followed by the four
exact Reckless delivery hashes with every admission flag still disabled.
The embedded 146-test result must pass exactly, every artifact member is bounded
and hashed, and the four native and delivery identities must remain unique.

All four recordings remain listening-unapproved and excluded from the game
catalogue, defaults and Recording mode. Content ID, vocals, explicit content,
full-track listening, transitions, warning audibility and game suitability
remain pending. The source table's loop-end sample positions are retained only
as `referenceLoopEndSeconds`; hosted decoding supplies authoritative complete
file duration evidence. Each generated row uses the accepted structured-rights
schema: CC BY 4.0 identity, exact source/licence evidence URL, public attribution
equal to its credit, truthful OGG-to-MP3 derivative disclosure, and explicit
null ShareAlike delivery fields.

Hosted assembly must preserve all 136 accepted catalogue entries byte-for-byte
and in order, add exactly four unique recordings, run the complete production
verifier, and push only generated archive metadata and audio to this draft
publication branch. Independent review and public byte/playback verification
remain required before merge.

The hosted workflow rejects every generated path outside the exact Reckless
batch/archive trees and the three required root metadata files. It stages those
paths explicitly, requires no remaining worktree changes, validates the cached
path set, commits, requires a clean checkout, and reruns the production verifier
against the committed HEAD before pushing.

The original intake workflow classifies the working catalogue through the same
exact base/generated validator. It reacquires and re-encodes the source audio
only from the exact 136-row base. After the publication bot adds the reviewed
140-row output, the workflow still runs the complete source, catalogue and
production verification but skips runtime checkout, tool installation,
acquisition and artifact upload instead of mistaking the intended four rows for
preexisting source duplicates.
